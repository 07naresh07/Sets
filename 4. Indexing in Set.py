mySet = {1, 3, 2, 5, 4, 2}
#Indexing using mySet[0] does not support set method, we can access using items
print(1 in mySet)
#Getting length of set
print(len(mySet))   #Only counts the length of unique elements